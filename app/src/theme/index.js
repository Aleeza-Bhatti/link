const { colors, gradients } = require('./colors');
const { spacing, radii } = require('./spacing');
const { typography } = require('./typography');

module.exports = {
  colors,
  gradients,
  spacing,
  radii,
  typography,
};
